<h1>Products#index</h1>
<%= cart.each do |product| %>
  <p> <%= product %> </p>
<% end %>

<%= form_tag('/products', action:"/product/add", method: "post") do %>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart" %>
<% end %>
