<ul>
  <%= cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>

<h3> Add an Item to your cart </h3>
<%= form_tag %>

  <input name=product>
  <input type=submit value='add to cart'>
