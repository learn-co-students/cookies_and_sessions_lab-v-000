<%= form_tag({action: "add", method: "post", controller: "products"}) do %>
  <%= label_tag 'product' %>
  <%= text_field_tag 'product' %>
  <%= submit_tag 'add to cart' %>
<% end %>

<ul>
  <% @cart.each do |item| %>
  <li><%= item %></li>
  <% end %>
</ul>
