<h1>Shopping Cart</h1>
<ul>
<%= cart.each do |item| %>
    <li><%= item %></li>  
<% end %>
</ul>
<h2>Add a product</h2>

<%= form_tag("/", method: "post", action: "add") do %>
    <%= text_field_tag(:product) %>
    <%= submit_tag("add to cart") %>
  <% end %>