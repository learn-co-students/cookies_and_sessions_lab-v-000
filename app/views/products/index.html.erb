<% if @cart.empty? %>
  <h2>Nothing in your cart.</h2>
<% else %>
  <h2>Your cart:</h2>
  <ul>
  <% @cart.each do |item| %>
    <li><%= item %></li>
  <% end %>
  </ul>
<% end %>


<%= render 'form' %>