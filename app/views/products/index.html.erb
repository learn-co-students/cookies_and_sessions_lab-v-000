<h2>Products</h2>

<% if session[:cart] %>
  <% session[:cart].each do |item|  %> 
    <%= item %>
  <% end %>
<% end %>
  
<%= form_tag('/products/add' ) do |f| %>
  <%= text_field_tag "product" %>
  <%= button_tag 'add to cart'%>
<% end %>