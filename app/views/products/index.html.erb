<h1> Cart Stuff </h1>

<ul>
  <% cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>


<h1> New Cart Stuff </h1>

<%= form_tag products_add_path do %>
  <label>Product: <%= text_field_tag 'product' %></label>
  <%= submit_tag 'add to cart' %>
<% end %>
