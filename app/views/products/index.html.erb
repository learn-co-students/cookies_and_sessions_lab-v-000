<%= form_tag(add_path, method: :post) do |f| %>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart" %>
<% end %>

<ul>
  <% cart.each do |item| %>
    <li>
      <%= item %>
    </li>
  <% end %>
</ul>