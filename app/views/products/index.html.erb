
<%= form_tag '/products/add', method: 'post' do |i| %>
	<%= label_tag 'product' %>
	<%= text_field_tag 'product' %>

	<%= submit_tag 'add to cart' %>
<% end %>

<% if !session[:cart].nil? %>
	<ul>
		<% session[:cart].each do |item| %>
			<li><%= item%></li>
		<% end %>
	</ul>
<% end %>