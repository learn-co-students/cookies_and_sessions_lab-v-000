<h1>Add to Products Cart</h1>

<% if !@cart.empty? %>
  <% @cart.each do |item| %>
  <%= item %>
  <% end %>
<% end %>

<%= form_tag(method: :post, action: :create, controller: :products) do %>

<label>Product</label><br>
<%= text_area_tag :product %><br>
<%= submit_tag "add to cart" %>
<% end %>
