<h1>Your Cart</h1>

<% if @cart %>
  <% @cart.each do |item| %>
    <li><%= item %></li>
  <% end %>
<% end %>

<h2>Add a Product</h2>

<%= form_tag('/', method: :post) do %>
  <%= label_tag :product %>
  <%= text_field_tag :product %>
  <%= submit_tag "add to cart" %>
<% end %>
