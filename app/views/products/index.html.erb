<h1>Products</h1>

<ul>
  <% cart.each do |item| %>
    <li>
      <%= item %>
    </li>
  <% end %> 
</ul>

<%= form_tag products_path do %>
  <%= text_field_tag :product %>
  
  <%= submit_tag "add to cart" %>
<% end %>

  