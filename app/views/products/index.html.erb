<h1>Add a product to your cart</h1>
<%= form_tag('/add') do %>
  <%= text_field_tag 'product' %>
  <%= submit_tag 'add to cart' %>
<% end %>

<h1>Shopping Cart</h1>
<ul>
  <%= @cart.each do |item| %>
    <li><% item %></li>
  <% end %>
</ul>
