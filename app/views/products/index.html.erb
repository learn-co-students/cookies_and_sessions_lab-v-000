<%= form_tag products_add_path do %>
	<%= text_field_tag(:product) %>
	<%= submit_tag "add to cart" %>
<% end %>

<% if session[:cart] %>
	<h5>Current Cart</h5>
	<% session[:cart].each do |item| %>
		<p><%= item %></p>
	<% end %>
<% end %>