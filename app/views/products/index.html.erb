<h1>Products#index</h1>

<%= form_tag(products_path, method: :post) %>
<%= label_tag 'Product: ' %>
<%= text_field_tag 'product' %>
<%= submit_tag "add to cart" %>

<p>Your cart:</p>
<% @cart.each do |item| %>
  <p><%= item %></p>
<% end %>

