<h1>I hate rails</h1>

<%= form_tag("/products", method: 'post') do %>
  <%= label_tag(:product, "Add a random item:") %>
  <%= text_field_tag 'product' %>

  <%= submit_tag 'add to cart' %>
<% end %>


<h2>Items in session cookie:</h2>
<ul>
  <% @cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>