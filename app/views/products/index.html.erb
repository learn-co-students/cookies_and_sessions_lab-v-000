<%= form_tag 'add' do %>
  <% label_tag(:product, "Add to cart:") %>
  <%= text_field_tag(:product) %><br>
  <br>
  <%= submit_tag('add to cart')%>
<% end %>

<ul>
  <% cart.each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>
